# 超级简化的 Dockerfile - 只使用 HTTP
FROM mcr.microsoft.com/dotnet/aspnet:8.0

WORKDIR /app

# 只暴露 HTTP 端口
EXPOSE 5074

# 复制发布的应用
COPY publish/ .

# 设置环境变量 - 只使用 HTTP
ENV ASPNETCORE_ENVIRONMENT=Production
ENV ASPNETCORE_URLS=http://+:5074

# 设置权限
RUN chmod +x ./DNSApi

# 启动应用
ENTRYPOINT ["./DNSApi"]